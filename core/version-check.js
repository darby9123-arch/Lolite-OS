/* Lolite OS 3.6.0 version compatibility check. */
(()=>{const CURRENT_VERSION='3.6.1',KEY='lolite-os-version';const compare=(a,b)=>{const A=String(a).split('.').map(Number),B=String(b).split('.').map(Number);for(let i=0;i<Math.max(A.length,B.length);i++){const x=A[i]||0,y=B[i]||0;if(x!==y)return x-y}return 0};const show=()=>{const old=document.getElementById('lolite-update-warning');if(old)old.remove();const el=document.createElement('div');el.id='lolite-update-warning';el.innerHTML=`<div class="update-warning-card"><div class="update-warning-icon">↗</div><div class="update-warning-copy"><strong>A newer version of Lolite OS is available</strong><span>You're using an older version. Update to get the latest apps, UI improvements and platform features.</span><small>Latest version: ${CURRENT_VERSION}</small></div><button id="lolite-update-dismiss">Later</button></div>`;document.body.appendChild(el);document.getElementById('lolite-update-dismiss')?.addEventListener('click',()=>el.remove())};const run=()=>{const saved=localStorage.getItem(KEY);if(saved&&compare(saved,CURRENT_VERSION)<0)show();localStorage.setItem(KEY,CURRENT_VERSION)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run);else run();window.LoliteVersion={current:CURRENT_VERSION,check:run}})();